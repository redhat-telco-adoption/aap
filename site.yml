- name: Test
  hosts: web
  tasks:
    - name: Fetch file
      ansible.builtin.fetch:
        src: /tmp/test.txt
        dest: test.txt
        flat: true
      register: file
    - name: Debug
      ansible.builtin.debug:
        var: file
      